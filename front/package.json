{
  "name": "front",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "pureheroky",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n TSC,WEB \"npm:tsc:watch\" \"npm:serve\"",
    "tsc:watch": "tsc -p . -w",
    "build": "rimraf dist && tsc -p .",
    "serve": "live-server --quiet --watch=./dist,./index.html --open=./index.html",
    "lint": "echo \"(опционально) добавь eslint/prettier позже\""
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "live-server": "^1.2.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
